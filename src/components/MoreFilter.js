import React from 'react';
import { connect } from 'react-redux'
import { addMore } from './../actions/featureActions'

const MoreFilter = props => {
    return (
        <li>
            {}
        </li>
    )
}